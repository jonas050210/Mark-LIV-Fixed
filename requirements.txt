# Complete MARK LIV installation. Individual groups are split so contributors
# and packagers can install only the surface they are working on. setup.py uses
# this aggregate to preserve the existing full-featured installation behavior.
-r requirements-core.txt
-r requirements-dashboard.txt
-r requirements-desktop.txt
-r requirements-plugins.txt
